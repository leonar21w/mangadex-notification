package constants

import "time"

const LoginInterval = 24 * 28 * time.Hour
const AccessTokenRefreshInterval = 20 * time.Minute
const FollowedMangaListenerInterval = 3 * time.Hour
const FeedListenerInterval = 3 * time.Minute
